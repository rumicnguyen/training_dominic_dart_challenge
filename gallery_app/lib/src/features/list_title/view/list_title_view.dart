import 'package:flutter/cupertino.dart';

class ListTitleView extends StatelessWidget {
  const ListTitleView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('ListTitleView');
  }
}