import 'package:flutter/cupertino.dart';

class WrapChipView extends StatelessWidget {
  const WrapChipView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('WrapChipView');
  }
}