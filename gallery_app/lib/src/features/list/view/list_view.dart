import 'package:flutter/cupertino.dart';

class XListView extends StatelessWidget {
  const XListView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('XListView');
  }
}